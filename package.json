{
  "name": "koa-pass-server",
  "version": "1.0.0",
  "description": "Node server running koa and using passport for authentication",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "run": "node --harmony app.js"
  },
  "keywords": [
    "koa",
    "passport",
    "authentication"
  ],
  "author": "Max Daunarovich",
  "license": "ISC",
  "dependencies": {
    "koa": "^1.2.0",
    "koa-router": "^5.4.0"
  }
}
